<%= content_for :title_section do %>
  <title>Private Chats</title>
<% end %>

<div class="relative w-full sm:w-full xl:w-desktop h-full w-70 m-auto overflow-hidden">
  <div class="w-full h-full overflow-y-scroll overflow-x-hidden bg-gray-100">
    <%= render 'chats/nav' %>

    <div class="h-full">
      <%= render partial: 'chats/chat', collection: @chats %>
    </div>
  </div>
</div>
